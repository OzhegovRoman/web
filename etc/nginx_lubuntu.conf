# Default server configuration
#
server {
    	listen 80 default;
    	listen [::]:80 default;
	location ~* \Q/uploads/\E\w+ {
      		root /home/roman/web;
    	}
    	location / {
      		return 404;
    	}
    	location ~* ^.+\.\w+$ {
      		root /home/roman/web/public;
    	}
}
